type Toolkit {
  id: ID!
  title: String!
  description: String
  description_markdown: String
  canvas: String
  learning: String
}

type Query {
  hello: String
  toolkits: [Toolkit]
  toolkit(id: ID!): Toolkit
}

schema {
  query: Query
}
